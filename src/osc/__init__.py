"""OSC helpers that are not the X AIR UDP client itself.

Mixer I/O lives in ``src.xair_control``. This package holds optional bridges
(REAPER, later surfaces) so they stay out of the audio UDP path.
"""
